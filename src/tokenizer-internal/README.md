# tokenizer-internal

Private tokenizer implementation modules.

- Not part of the public API.
- `src/tokenizer.ts` is the stable entrypoint.
- This folder contains tokenizer concerns split out for maintainability (currently token type declarations).